export function initMonitoring() {
  // TODO: integrate Sentry or another monitoring service.
  // Example (Sentry):
  // import * as Sentry from '@sentry/nextjs';
  // Sentry.init({ dsn: process.env.SENTRY_DSN });
  console.log('Monitoring initialized (placeholder)');
}
